name: communication_demo
description: A Flutter communication demo with chat, image, voice sharing, and real-time updates.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  socket_io_client: ^2.0.0
  image_picker: ^0.8.7+4
  flutter_sound_lite: ^8.4.6
  permission_handler: ^10.4.0
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true